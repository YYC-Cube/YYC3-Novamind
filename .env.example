# YYC³ NovaMind 环境变量模板
# 复制为 .env.local 并填入真实值；.env* 已在 .gitignore 中，严禁提交真实密钥

# ── Sentry 错误监控（P0-③）──────────────────────────
# 缺省时 Sentry 完全禁用（零开销）；填入 DSN 后自动启用
# 前端（浏览器）错误上报
NEXT_PUBLIC_SENTRY_DSN=
# 服务端（RSC / Route Handlers）错误上报
SENTRY_DSN=
# 环境标识（缺省取 NODE_ENV）
NEXT_PUBLIC_SENTRY_ENV=
SENTRY_ENV=

# ── AI 服务（可选）─────────────────────────────────
OPENAI_API_KEY=
OPENAI_BASE_URL=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
LOCAL_AI_URL=
LOCAL_AI_KEY=
